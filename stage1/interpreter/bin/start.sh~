#
# Check to see crucial environment variables are set. Set to defaults if needed
#
if [ "${ipaddr}x" == "x" ]; then ipaddr=127.0.0.1; fi
if [ "${hostname}x" == "x" ]; then hostname='localhost'; fi
#
# Run the Interpreter
#
/flask/bin/python3 interpreter.py

